# Bước 1 — Document Loading & Chunking
# Cài đặt: pip install -r requirements_step1.txt

langchain>=0.2.0
langchain-community>=0.2.0
pypdf>=4.0.0          # PyPDFLoader dùng thư viện này để đọc PDF
reportlab>=4.0.0      # Chỉ dùng để tạo PDF giả — xóa nếu có file thật
